# 📚 ДОКУМЕНТАЦИЯ ПРОЕКТА \"СЕКРЕТАРЬ КЛАВА\"

## Состояние на 25 февраля 2026, 12:00 UTC

## 1. 🚀 Быстрый старт (подключение)

### Подключение к серверу

bash

ssh root@147.45.146.53

*\# пароль: (ваш пароль)*

### Переход в проект

bash

cd /root/ai-secretary/cyberkitty119

### Проверка статуса

bash

docker ps \| grep bot

docker logs cyberkitty19-transkribator-bot \--tail 20

## 2. ✅ Текущий статус

### Что работает:

- ✅ **Бот стабильно работает** (контейнер в статусе Up)

- ✅ **Распознавание речи** (faster-whisper, модель small, 4 потока)

- ✅ **DeepSeek API** (ключ активен, баланс есть)

- ✅ **База данных PostgreSQL** (таблицы tasks и users)

- ✅ **Проверка пользователя** (только для Ирины, Telegram ID:
  884710380)

- ✅ **Обработка голосовых сообщений** (бот отвечает)

### ⚠️ Текущая проблема:

**DeepSeek возвращает action = \'ask\' вместо \'add\'**, поэтому задачи
НЕ сохраняются в базу данных. Нужно доработать промпты.

## 3. 📁 Структура проекта (важные файлы)

text

/root/ai-secretary/cyberkitty119/

├── docker-compose.yml \# Конфигурация Docker

├── .env \# Переменные окружения (токены, ключи)

├── requirements.txt \# Зависимости Python

│

├── transkribator_modules/

│ ├── bot/

│ │ ├── handlers/

│ │ │ ├── voice_handler.py \# ОСНОВНОЙ: обработка голоса

│ │ │ └── \_\_init\_\_.py

│ │ └── commands.py \# Команды бота (/start)

│ │

│ ├── services/

│ │ └── deepseek_service.py \# Работа с DeepSeek API

│ │

│ ├── db/

│ │ ├── task_crud.py \# Работа с БД (обновлён)

│ │ ├── models.py \# Модели БД

│ │ └── database.py \# Подключение к БД

│ │

│ ├── prompts/

│ │ └── secretary_prompts.py \# Промпты для DeepSeek (нужно создать)

│ │

│ └── main.py \# Точка входа

## 4. 🔑 Ключевые переменные (.env)

bash

*\# Telegram*

TELEGRAM_BOT_TOKEN=8613461310:AAE35djYcy-tCUbn_bncNAFz11oobgoiPts

*\# DeepSeek*

DEEPSEEK_API_KEY=sk-44993312f65c47dc9b037ea6d029a436

DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

*\# PostgreSQL*

DATABASE_URL=postgresql+psycopg://postgres:Avalon21@postgres:5432/secretary_db

POSTGRES_DB=secretary_db

POSTGRES_USER=postgres

POSTGRES_PASSWORD=Avalon21

*\# ID пользователя*

USER_TELEGRAM_ID=884710380 *\# Ирина*

## 5. 🗄️ База данных

### Таблицы:

sql

tasks (

id SERIAL PRIMARY KEY,

user_id INTEGER REFERENCES users(id),

type VARCHAR(50),

category VARCHAR(50),

title VARCHAR(500),

description TEXT,

original_text TEXT,

created_at TIMESTAMP,

deadline TIMESTAMP,

priority VARCHAR(20),

is_completed BOOLEAN,

is_active BOOLEAN,

participants TEXT,

location VARCHAR(500),

action_items TEXT

)

users (

id SERIAL PRIMARY KEY,

telegram_id INTEGER UNIQUE,

username VARCHAR(255),

first_name VARCHAR(255),

created_at TIMESTAMP,

timezone VARCHAR(50)

)

### Проверка БД:

bash

*\# Все задачи*

docker exec -it cyberkitty19-postgres psql -U postgres -d secretary_db
-c \"SELECT \* FROM tasks ORDER BY id DESC LIMIT 5;\" -P pager=off

*\# Последняя задача*

docker exec -it cyberkitty19-postgres psql -U postgres -d secretary_db
-c \"SELECT \* FROM tasks ORDER BY id DESC LIMIT 1;\" -P pager=off -x

## 6. 🐳 Docker команды

bash

*\# Статус контейнеров*

docker ps \| grep bot

*\# Логи бота*

docker logs cyberkitty19-transkribator-bot \--tail 50

docker logs cyberkitty19-transkribator-bot -f *\# в реальном времени*

*\# Перезапуск бота*

docker-compose stop bot

docker-compose up -d bot

*\# Полная перезагрузка*

docker-compose down

docker-compose up -d

*\# Пересборка с очисткой*

docker-compose build \--no-cache bot

docker-compose up -d bot

## 7. 📝 Текущая задача (ЧТО НУЖНО СДЕЛАТЬ)

### Проблема: DeepSeek возвращает action=\'ask\' вместо \'add\'

### Решение:

1.  **Создать файл с промптами** (уже сделано, но нужно проверить)

> bash
>
> nano transkribator_modules/prompts/secretary_prompts.py

2.  **Обновить voice_handler.py** для использования нового промпта

> bash
>
> nano transkribator_modules/bot/handlers/voice_handler.py
>
> Найти строку с personal_prompt и заменить на:
>
> python
>
> from \...prompts.secretary_prompts import MAIN_PROMPT
>
> personal_prompt = MAIN_PROMPT

3.  **Перезапустить бота**

> bash
>
> docker-compose stop bot
>
> docker-compose up -d bot

4.  **Протестировать**

    - Отправить голосовое: \"Купить яблоки\"

    - Проверить логи: docker logs cyberkitty19-transkribator-bot \--tail
      30 \| grep -i \"action\\\|add\"

    - Проверить БД: docker exec -it cyberkitty19-postgres psql -U
      postgres -d secretary_db -c \"SELECT \* FROM tasks ORDER BY id
      DESC LIMIT 1;\" -P pager=off -x

## 8. 🔜 Следующие шаги (после решения текущей проблемы)

1.  **Команда /tasks** --- просмотр всех задач

2.  **Команда /today** --- задачи на сегодня

3.  **Команда /done** --- отметить задачу выполненной

4.  **Система напоминаний** --- автоматические уведомления

## 9. 📊 Полезные команды для диагностики

bash

*\# Проверить, что DeepSeek отвечает*

curl https://api.deepseek.com/v1/chat/completions \\

-H \"Authorization: Bearer \$DEEPSEEK_API_KEY\" \\

-H \"Content-Type: application/json\" \\

-d \'{

\"model\": \"deepseek-chat\",

\"messages\": \[{\"role\": \"user\", \"content\": \"Купить молоко\"}\]

}\'

*\# Проверить логи ошибок*

docker logs cyberkitty19-transkribator-bot 2\>&1 \| grep -i error

*\# Проверить использование памяти*

docker stats \--no-stream

*\# Зайти в контейнер*

docker exec -it cyberkitty19-transkribator-bot /bin/bash

## 10. 📂 Резервные копии

bash

*\# Бэкап проекта*

cd /root/ai-secretary

tar -czf backup-\$(date +%Y%m%d).tar.gz cyberkitty119/

*\# Бэкап базы данных*

docker exec cyberkitty19-postgres pg_dump -U postgres secretary_db \>
db-backup-\$(date +%Y%m%d).sql
