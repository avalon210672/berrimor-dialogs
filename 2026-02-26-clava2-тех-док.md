# 📚 ТЕХНИЧЕСКАЯ ДОКУМЕНТАЦИЯ: TELEGRAM БОТ \"СЕКРЕТАРЬ КЛАВА\"

## Версия: Clava1.1 (расширенная)

## Дата актуализации: 26 февраля 2026, 12:00 UTC

## 1. Что нового в версии Clava1.1

### ✅ Добавленные функции:

- **Защита от дубликатов** - автоматическое обнаружение повторяющихся
  задач (24 часа)

- **Категоризация покупок** - правильное определение и сохранение
  списков покупок

- **Расширенные команды просмотра** - /categories и /category

- **Поддержка двух пользователей** - Сергей и Ирина

- **Поле items в БД** - для хранения списков покупок

### ✅ Исправленные проблемы:

- Синтаксические ошибки в нескольких файлах

- Неправильные отступы в Python-коде

- Отсутствие колонки items в таблице tasks

- Проблемы с импортами модулей

- Ошибка \"INSERT has more target columns than expressions\"

- Ошибка \"unexpected indent\" в category_crud.py

## 2. Новые модули и функции

### 2.1. Защита от дубликатов (category_crud.py)

python

def check_similar_purchase(self, user_telegram_id: int, items: list,
hours: int = 24) -\> bool:

\"\"\"

Специальная проверка для покупок по списку товаров

\"\"\"

logger.info(f\"🔍 check_similar_purchase вызван:
user={user_telegram_id}, items={items}\")

try:

user = self.db.query(User).filter(User.telegram_id ==
user_telegram_id).first()

if not user or not items:

return False

time_threshold = datetime.now() - timedelta(hours=hours)

*\# Очищаем новые items*

new_items_set = set()

for item in items:

clean_item = item.lower().strip().rstrip(\'.,!?;:\').strip()

if clean_item:

new_items_set.add(clean_item)

*\# Ищем покупки за последние N часов*

purchases = self.db.query(Task).filter(

Task.user_id == user.id,

Task.category == \"покупки\",

Task.is_active == True,

Task.created_at \>= time_threshold

).all()

for purchase in purchases:

*\# Проверяем наличие items в разных форматах*

purchase_items = None

if hasattr(purchase, \'items\') and purchase.items:

purchase_items = purchase.items

elif hasattr(purchase, \'action_items\') and purchase.action_items:

purchase_items = purchase.action_items

if purchase_items:

*\# Парсим JSON если это строка*

if isinstance(purchase_items, str):

try:

purchase_items = json.loads(purchase_items)

except:

purchase_items = \[\]

existing_items_set = set()

for existing_item in purchase_items:

clean_existing =
str(existing_item).lower().strip().rstrip(\'.,!?;:\').strip()

if clean_existing:

existing_items_set.add(clean_existing)

if existing_items_set == new_items_set:

logger.info(f\"✅ Найден точный дубликат!\")

return True

if new_items_set.issubset(existing_items_set):

logger.info(f\"✅ Новые товары уже есть в списке\")

return True

return False

except Exception as e:

logger.error(f\"Ошибка при проверке дубликата покупок: {e}\")

return False

finally:

self.close()

### 2.2. Команды для просмотра задач (commands.py)

python

async def categories_command(update: Update, context:
ContextTypes.DEFAULT_TYPE):

\"\"\"Показать задачи по категориям\"\"\"

user_id = update.effective_user.id

*\# Проверка доступа для всех разрешенных пользователей*

allowed_users = \[

int(os.getenv(\"USER_TELEGRAM_ID\", \"0\")),

int(os.getenv(\"USER2_TELEGRAM_ID\", \"0\"))

\]

if user_id not in allowed_users:

await update.message.reply_text(\"⛔ Доступ запрещен\")

return

try:

*\# Получаем параметр - количество дней (по умолчанию 7)*

args = context.args

days = 7

if args and args\[0\].isdigit():

days = int(args\[0\])

*\# Получаем задачи по категориям*

from ..db.category_crud import CategoryService

service = CategoryService()

categories = service.get_tasks_by_category(user_id, days)

if not categories or all(len(tasks) == 0 for tasks in
categories.values()):

await status_msg.edit_text(f\"📭 Нет задач за последние {days} дней\")

return

*\# Формируем красивое сообщение с эмодзи*

emoji_map = {

\"покупки\": \"🛒\",

\"работа\": \"💼\",

\"личное\": \"👤\",

\"встреча\": \"📅\",

\"напоминание\": \"⏰\",

\"заметка\": \"📝\",

\"здоровье\": \"🏥\",

\"финансы\": \"💰\",

\"другое\": \"📌\"

}

*\# \... формирование сообщения \...*

except Exception as e:

logger.error(f\"Ошибка в categories_command: {e}\")

await update.message.reply_text(\"❌ Произошла ошибка\")

## 3. Обновлённая структура базы данных

### Таблица users (обновлённая):

sql

id SERIAL PRIMARY KEY,

telegram_id INTEGER UNIQUE,

username VARCHAR(255),

first_name VARCHAR(255),

last_name VARCHAR(255),

created_at TIMESTAMP,

timezone VARCHAR(50),

current_plan VARCHAR(50) DEFAULT \'free\',

plan_expires_at TIMESTAMP,

total_minutes_transcribed INTEGER DEFAULT 0,

minutes_used_this_month INTEGER DEFAULT 0,

last_reset_date TIMESTAMP,

total_generations INTEGER DEFAULT 0,

generations_used_this_month INTEGER DEFAULT 0,

updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

is_active BOOLEAN DEFAULT true,

beta_enabled BOOLEAN DEFAULT false,

google_connected BOOLEAN DEFAULT false

### Таблица tasks (обновлённая):

sql

id SERIAL PRIMARY KEY,

user_id INTEGER REFERENCES users(id),

type VARCHAR(50),

category VARCHAR(50),

title VARCHAR(500),

description TEXT,

original_text TEXT,

created_at TIMESTAMP,

deadline TIMESTAMP,

priority VARCHAR(20),

is_completed BOOLEAN DEFAULT false,

is_active BOOLEAN DEFAULT true,

participants TEXT,

location VARCHAR(500),

action_items TEXT,

items TEXT *\-- НОВОЕ ПОЛЕ для списков покупок*

## 4. Удачные приёмы и решения

### 🏆 ТОП-5 успешных решений:

1.  **Принудительная конвертация покупок**

> python
>
> if \'купить\' in transcript.lower():
>
> analysis\[\'category\'\] = \'покупки\'
>
> analysis\[\'type\'\] = \'purchase\'
>
> *\# Извлечение items из текста*
>
> items = text_after_buy.replace(\' и \', \', \').split(\', \')
>
> *Почему удачно:* Обходит капризы LLM и гарантирует правильную
> категоризацию.

2.  **Централизованная конфигурация действий (actions_config.py)**

> python
>
> ACTION_HANDLERS: Dict\[str, Callable\] = {}
>
> def register_handler(action: str):
>
> def decorator(func):
>
> ACTION_HANDLERS\[action\] = func
>
> return func
>
> return decorator
>
> *Почему удачно:* Единое место для всех действий, легко добавлять
> новые.

3.  **Защита от дубликатов с очисткой знаков препинания**

> python
>
> clean_item = item.lower().strip().rstrip(\'.,!?;:\').strip()
>
> *Почему удачно:* \"бананы\", \"бананы.\", \"бананы,\" считаются
> одинаковыми.

4.  **Безопасное редактирование сообщений**

> python
>
> async def safe_edit_text(status_msg, new_text):
>
> if status_msg.text != new_text:
>
> await status_msg.edit_text(new_text)
>
> *Почему удачно:* Избегает ошибки \"Message is not modified\".

5.  **Поддержка двух пользователей через переменные окружения**

> python
>
> allowed_users = \[
>
> int(os.getenv(\"USER_TELEGRAM_ID\", \"0\")),
>
> int(os.getenv(\"USER2_TELEGRAM_ID\", \"0\"))
>
> \]
>
> *Почему удачно:* Легко добавлять новых пользователей без изменения
> кода.

## 5. Извлечённые уроки и проблемы

### ⚠️ Проблемы, с которыми столкнулись:

1.  **Синтаксические ошибки из-за копирования кода**

    - Лишние символы (џ\"\"\", expert вместо except)

    - Неправильные отступы (пустые строки с пробелами)

    - *Решение:* Всегда проверять синтаксис перед сохранением

2.  **Проблемы с импортами**

    - from ..db.category_crud vs from \...db.category_crud

    - *Решение:* Использовать полные пути или проверять структуру пакета

3.  **Отсутствие поля в БД**

    - Добавили items в таблицу, но забыли в INSERT запросе

    - *Решение:* Всегда синхронизировать модель и запросы

4.  **Вложенные функции из-за отступов**

    - Функции оказывались внутри других функций

    - *Решение:* Визуально проверять уровни отступов

## 6. Рекомендации для продолжения работы

### 🔜 Следующие шаги (приоритетные):

1.  **Команда /today** - задачи на сегодня

> python
>
> async def today_command(update: Update, context:
> ContextTypes.DEFAULT_TYPE):
>
> *\# Получить задачи с дедлайном = сегодня*
>
> tasks = task_service.get_tasks_by_date(user_id, datetime.now())

2.  **Команда /done \[ID\]** - отметить задачу выполненной

> python
>
> async def done_command(update: Update, context:
> ContextTypes.DEFAULT_TYPE):
>
> task_id = context.args\[0\]
>
> task_service.complete_task(task_id)

3.  **Система напоминаний** - фоновые уведомления

    - Добавить apscheduler jobs

    - Проверять приближающиеся дедлайны

4.  **Очистка старых тестовых данных**

> sql
>
> DELETE FROM tasks WHERE category=\'личное\' AND title LIKE
> \'%Купить%\';

### 💡 Идеи для улучшения:

1.  **Кэширование результатов распознавания** - ускорить повторные
    запросы

2.  **Web-интерфейс** - просмотр задач через браузер

3.  **Экспорт задач** - в CSV, Google Calendar

4.  **Голосовые ответы** - синтез речи для ответов

5.  **Множественные списки покупок** - \"продукты\", \"аптека\",
    \"хозтовары\"

## 7. Обновлённые команды для администрирования

### Проверка статуса и логов:

bash

*\# Статус контейнеров*

docker ps \| grep bot

*\# Логи с фильтром по дубликатам*

docker logs cyberkitty19-transkribator-bot \--tail 200 \| grep -i
\"дубликат\\\|check_similar_purchase\\\|сохранена\"

*\# Логи ошибок*

docker logs cyberkitty19-transkribator-bot 2\>&1 \| grep -i error

### Работа с базой данных:

bash

*\# Проверить последние задачи с items*

docker exec -it cyberkitty19-postgres psql -U postgres -d secretary_db
-c \"SELECT id, title, items FROM tasks ORDER BY id DESC LIMIT 5;\" -P
pager=off

*\# Очистить старые тестовые данные*

docker exec -it cyberkitty19-postgres psql -U postgres -d secretary_db
-c \"DELETE FROM tasks WHERE id \< 80 AND items IS NULL;\" -P pager=off

*\# Статистика по категориям*

docker exec -it cyberkitty19-postgres psql -U postgres -d secretary_db
-c \"SELECT category, COUNT(\*) FROM tasks GROUP BY category;\" -P
pager=off

### Работа с контейнером:

bash

*\# Войти в контейнер*

docker exec -it cyberkitty19-transkribator-bot /bin/bash

*\# Проверить синтаксис Python файла*

python3 -m py_compile /app/transkribator_modules/db/category_crud.py

*\# Перезапустить с полной пересборкой*

docker-compose down

docker-compose up -d \--build

## 8. Финальные проверки перед релизом

### ✅ Чек-лист работоспособности:

- Бот запускается без ошибок

- Команда /start работает

- Голосовые сообщения распознаются

- DeepSeek возвращает правильные категории

- Покупки попадают в категорию \"покупки\"

- Items сохраняются в базу данных

- Защита от дубликатов работает

- Команда /categories показывает задачи

- Команда /category фильтрует по категории

- Оба пользователя имеют доступ

## 9. Заключение

Проект \"Секретарь Клава\" достиг стабильного состояния с версией
Clava1.1.\
Основные функции работают надёжно, добавлена защита от дубликатов и\
удобный просмотр задач по категориям. Кодовая база очищена от\
синтаксических ошибок, структурирована и готова к дальнейшему развитию.

**Ключевые достижения:**

- ✅ Полностью функциональный голосовой помощник

- ✅ Интеллектуальная классификация задач

- ✅ Защита от дублирования

- ✅ Удобный интерфейс команд

- ✅ Масштабируемая архитектура

**Следующий этап:** добавление команд для управления задачами\
(/today, /done) и системы напоминаний.

*Документация составлена: 26 февраля 2026, 12:00 UTC*
